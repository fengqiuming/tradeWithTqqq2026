__pycache__/
*.py[cod]
*.egg-info/
.DS_Store
*.tmp
*.swp

# 工作区记忆 / 工具私有目录，不入库
.workbuddy/

# 凭据 —— 绝不入库。.env 存 GitHub token 等敏感信息，
# 只提交 .env.example 作为模板。push_github.sh 启动时会校验 .env 未被 git 跟踪。
.env
.env.*
!.env.example
